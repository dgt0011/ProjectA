server {
       listen 80;
       listen [::]:80;

       server_name projects.darrentuer.net;
       client_max_body_size 100M;

       root /mnt/data/web;

       location / {
               try_files $uri $uri/ =404;
       }
}